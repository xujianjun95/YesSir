# 更新日志

本项目所有值得记录的版本变更都记录在此。版本号遵循语义化版本（与 `manifest.json` 同步）。

## [1.5.4] - 2026-06-03

### 修复
- **分类 pill 以真实 Chrome 标签组为准**：pill 不再依赖易失的 AI 快照缓存，改读 tab 真实所属标签组标题，修复重启浏览器 / 恢复会话 / 手动建组后「标签栏有分组但搜索框下方 pill 消失」的问题。
- **AI 分组「零碎浏览黑洞」**：移除会污染偏好的 `tabGroups.onCreated` 预登记，新增保留名屏蔽（零碎浏览 / 未分组 / Misc），并清洗历史脏偏好，杜绝「越分越多标签被吸进零碎浏览」的恶性循环；脏缓存命中自动作废重走 LLM。
- **IP / 内网站点品牌名**：IP、localhost、内网主机不再被送去 LLM 猜品牌而显示「无法确认品牌」，统一归入「本地网页/其他」分组；拦截模型否定回答并净化历史脏快照。
- **置顶 dock 概率不显示**：移除 content 侧非法 `chrome.tabs.query` 调用；SW 启动清理失效置顶 tabId，面板初始化加双保险校验。
- **面板高度固定** `min(620px, 80vh)`，标签页少时不再收缩。
- **搜索栏 line-height 显式隔离**，防止宿主页 CSS 污染搜索框高度。

## [1.5.3] - 2026-06-02

### 新增
- **遥测埋点重构**：修复丢数问题，新增 panel_open / uninstall / 留存 / 分群等维度。
- **分析脚本增强**：analyze 新增「版本分布表」与「每日功能使用人数表」。

### 优化
- **AI 分组三处优化**：动态阈值、温度归零、新建组记忆。

### 修复
- 遥测 5xx 丢数、keydown 误触发 / 拦截（代码审查 P0）。
- `rename_group` 误统计——排除程序化标题更新。
- `hideSwitcher` 统一 disconnect，修复 ResizeObserver 泄漏。
- AI 快照缓存随 tab 关闭清理，避免 tab id 复用错配。

## [1.5.2] - 2026-05-17

### 新增
- **置顶 Dock**：面板左右各三个持久化固定槽，支持拖入 / 拖出 / 换位 / 点击跳转，含 AI 归纳标签展示与「三幕剧」动画；标签关闭时自动清除对应置顶槽。
- **新手引导重做**：新版本更新后自动弹出（首开一次、3 天后再提醒一次），新增置顶拖拽与拖拽重分类说明。
- **埋点扩充**：置顶相关埋点（pin_to_dock / unpin_from_dock）、面板打开用户数埋点。

### 优化
- **统一动画语言「雾化浮层系统」**：全局 easing、分速收尾（transform→opacity→filter）。
- AI 分组支持全窗口；AI 缓存加 TTL 清理 + fetch 全局超时。

### 修复
- 分类 pill 与浏览器标签组同步；过滤为空时 categoryBar 残留。
- 红绿灯高度 / 圆角异常、置顶槽 GPU 合成黑边、横向滚动等面板视觉问题。
